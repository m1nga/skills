interface:
  display_name: "Playlist Builder"
  short_description: "Build a verified playlist that imports without broken matches"
  default_prompt: "Use $mixtape to turn my mood or seed songs into an authored playlist, verify every track against my storefront, and keep future iterations under one stable library title."
