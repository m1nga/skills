# 在 Ming 的 Mac 上完成剩余验收

状态：这是本机执行入口，不是测试结果。创建此文件没有运行模型实验、子代理或更新用户电脑。

Ming 已要求继续完成，并要求只用简单的话解释实际帮助。执行者负责操作和记录，不让 Ming 逐项拼接命令。只在登录失效、权限不足、额外费用或本地修改冲突等确实需要本人决定的地方提问。

## 先明确要交付什么

最终回答三个问题：

- 换 Claude／Codex 或开新窗口时，TaskDock 是否减少找错版本、重复解释和返工？
- Ming 说“直接做”“只总结”“不要发布”时，会不会走错流程或误改文件？
- 这台 Mac 是否确实在用已修复的版本，day-close 是否独立保留且可恢复？

不要再做全库审美改写、恢复退役技能、引入遥测或新服务。没有可测收益也可以是结论。

## 1. 先保护本机，再获取代码

不要先对已有技能源码执行 git pull、reset 或清理。先检查实际安装位置和符号链接，重点是 ~/.claude/skills/day-close、~/.agents/skills/day-close；其他位置只按现有安装元数据定位，不扫描无关私人文件。

将 day-close 的实际内容（不是仅复制链接）和原链接信息保存在仓库之外的私有本地备份目录，使用唯一目录名、限制目录权限并逐文件核对哈希。已有定制优先保留。若它链接到即将被移除的源码，在保留原链接记录后，使用独立本地副本恢复安装，不覆盖未知文件。不要将备份、安装路径、个人笔记或登录信息提交到公共仓库。

如果本机已经缺少该技能，明确记录“本机缺失”；可从 cd4a5bd51ce49cf8bc8ee68cc98d0644e77a794e 中的 day-close/SKILL.md 和 day-close/agents/openai.yaml 恢复通用版本，但不能声称恢复了无法取得的用户定制。

保护完成后，在新的临时克隆中读取 m1nga/skills 的当前主分支和 docs/handoff/RECOVERY-AUDIT-2026-09-19.md。已有仓库有未提交修改时，不 reset、不强制 stash、不覆盖。更新只涉及本任务明确授权的安装项。

检查 TaskDock 的实际安装来源、版本和文件内容；使用该安装方式当前支持的更新方法，不同时增加重复的 plugin/skill 安装。已经是目标版本就不重装。以远端 SOURCE.json 和已验收源码为依据，不仅看版本字符串。重启受影响的宿主会话后再确认其看到的内容。在本机跑 TaskDock 适用测试；Mac runner 通过不代替本人电脑验证。

## 2. 登录、费用和权限：按本机实际情况确认

先运行 check-runtimes.py，读取本机 claude/codex 的 --help 和版本，并使用官方登录状态命令确认子会话可用。之前的报告记载 Ming 已完成 Claude 登录和 Python 修复；不要默认要求他重做。只在本次确实认证失败时让 Ming 在本机完成官方登录，不要求把密码、token、auth.json 或 API key 发到聊天、GitHub 或 CI。

默认只使用现有订阅包含的额度，不启用 extra usage、不购买额度、不自动切到按量付费 API。订阅额度不是无限的。若不能确认此次模型调用不会产生额外费用，在首次付费调用前只问一次“本轮允许的额外费用上限是多少？”，得到金额后设置可执行的累计停止条件。无法保证上限就不启动付费批次。不得因看见已有 API key 就自动使用它。

本轮先做每个条件一次的小样本，不自动扩成三轮。上限为每个宿主 3 条件 × 2 阶段，以及每个宿主 8 条触发用例；Claude 和 Codex 合计最多 28 个被测新会话。独立评审另限每宿主 1 个会话，也必须受同一费用边界约束。遇到配额、认证、隔离或权限问题，停止受阻部分并说明唯一需要的用户动作；继续不依赖它的工作，不盲目重试。

只给被测代理测试工作目录内必要的读写权限，禁止对真实项目、全局配置和公共远端的写入。不要为跑测试关闭系统安全限制。协调者的最终安装更新／脱敏结果提交权限不能传给被测代理。

截至本入口编写时，Claude 官方文档说明 --bare 不使用订阅 OAuth，因此不能把它当作“免费隔离”开关；Codex 官方非交互文档要求显式选择合适的 sandbox。以本机版本帮助和以下官方文档为准，不猜参数：

- https://code.claude.com/docs/en/headless
- https://developers.openai.com/codex/noninteractive
- https://developers.openai.com/codex/auth

## 3. 执行两个实验，不把实验设计当成果

先读本目录 README.md、trigger-cases.json，以及 taskdock/product-root/evals/continuity/README.md。复用已有 fixture 和验证函数，只补必要的宿主适配，不搭建新的评测平台。

连续任务：在每个宿主中分别跑普通宿主、简短工作约定、完整 TaskDock；同一宿主三组使用相同解析后的模型、输入、权限和预算。阶段 A 从原始材料开始，保存实际产物后彻底结束会话。按现有协议移动目录并注入相同的新证据；阶段 B 只拿持久文件和必要任务请求，不拿阶段 A 聊天、评审标准或预写答案。不同宿主的成本不能直接混成一个排名。

连续任务的三组须验证实际可见的技能清单：普通组和简短约定组不能偷偷继承 TaskDock。隔离不能靠临时删除用户全局技能、搬运凭证或声称“请忽略”完成。无法证明隔离就如实报告实验受阻；仍可继续不依赖隔离的本机安装和触发验收。

触发实验：在真实共装技能清单下，对八条中文请求分别启动新会话，两种宿主各跑一次。使用虚构临时项目，让“修改”“记录”“收尾”等请求有可操作对象。记录选中技能、实际副作用和最终回复；不要把为了测试而手动读 SKILL.md 当作自动触发成功。特别检查“不要动文件”“不要删除”“不要发布”的禁止项。

独立验收：验证文件字节、路径、支持的平台权限和实际执行的撤销，不仅检查回复是否出现 undo。测试完整恢复和遇到新修改时安全拒绝这两种情况。复制到临时验证目录后执行撤销时，校验结构化 argv 的写入目标确实是那份副本；不得因为原命令包含绝对路径而改到实际实验产物或用户文件。如确实需要调整工作目录参数，记录这种调整，并将“原样命令可执行性”单独验证，不混为同一个通过项。

语义验收由不含执行者聊天和自评结论的新读者查看事实、轨迹与预设标准；能启动真实子代理时用于独立检查，不能启动就明确记录，不伪称调用。不要用多个代理投票代替文件证据。

## 4. 结果回 GitHub，Ming 只看实际帮助

原始会话、费用明细、账户信息、本机路径和备份留在私有本地目录。记录真实 CLI/模型版本、源码提交、条件、独立会话 ID、token/turns、实际费用或未知值、各项客观结果、人工纠正次数。订阅中的等价 token 费用如需估算必须标为估算，未知不能填零。

只将虚构用例的脱敏结果和方法限制写入本目录 RESULTS-2026-09-19.md；已存在则先读取并追加，不覆盖别人的证据。状态用 passed/failed/blocked/not_run 分开，未执行的格子不补满。结果写回已有仓库，Ming 不必搬运完整日志。更新 docs/handoff/RECOVERY-AUDIT-2026-09-19.md 的剩余状态，但保留历史验证事实。提交前做脱敏检查，公共仓库不得放本机清单或个人文件内容。

一次小样本只能发现问题，不能证明普遍更快。若简短约定与完整 TaskDock 效果相同且成本不更高，建议普通工作不用完整技能；未证明差异就写“暂未证明”，不强行创造优势。只修实际复现的错误，实质产品修复仍按仓库既有审查／发布规则，不因测试要求擅自重写所有触发描述。

最终给 Ming 的中文回复不超过 250 字：电脑是否更新完成；实测减少了什么具体错误或返工；哪些技能值得常用、哪些只在需要时用；是否有额外费用；还有什么必须由本人完成。测试数量、提交和日志放在链接后的证据中，不作为用户价值本身。
