interface:
  display_name: "Side Task Assistant"
  short_description: "Capture side work without losing your main thread"
  default_prompt: "Use $side-quest to capture this side thought and keep my main thread going."
